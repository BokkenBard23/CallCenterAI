fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
lxml>=5.3.0
httpx>=0.28.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.1
striprtf>=0.0.26
pymorphy3>=0.2.0
pymorphy3-dicts-ru>=0.1.0

# ── FRIDA embedding integration ──
faiss-cpu>=1.7.4
numpy>=1.26.0
razdel>=0.5.0
natasha>=1.6.0
slowapi>=0.1.9
tenacity>=8.2.0

# ── Export (Excel, PDF) ──
openpyxl>=3.1.0
reportlab>=4.0.0

# ── Async SQLite (future async session store) ──
aiosqlite>=0.20.0

# ── PII masking (ID-4: Presidio + NER for 152-ФЗ compliance) ──
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# ── Optional: advanced morphological & semantic analysis ──
# These are NOT hard dependencies. The corresponding services degrade gracefully
# (logged warning + empty results) when the package is absent.
# gliner>=0.2          # optional, for app/services/domain_ner.py (zero-shot domain NER)
# networkx>=3.0        # optional, for app/services/topics.py (topic modeling via community detection)
